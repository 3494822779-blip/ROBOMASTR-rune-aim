# 视频回放（需 GPU 引擎 + 图形会话）
# 公共参数统一在 template.yaml（唯一参数源），本文件只写本场景差异。
# 相机内参默认用 template 的 1440×1080 演示值；实车请用 camera_sentry.yaml 的标定值。
input:
  mode: video
  source: data/rune_test_h264.mp4
  max_frames: 0

detect:
  engine: model/Rune-v8n-fp16-20260624_b1_gpu0_fp16.engine
