# 大符（正弦）+ 虚拟符（无 GPU）
# 公共参数统一在 template.yaml（唯一参数源），本文件只写本场景差异。
input:
  mode: virtual
  hz: 200

track:
  noise_observation: 2.0        # 虚拟观测无噪声，收紧帮助收敛

fire:
  bullet_speed: 24.5            # 与 virtual_rune 演示弹道匹配

virtual_rune:
  large: true                   # true = 大符（正弦）
